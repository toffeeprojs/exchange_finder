{
    "policies": [
        {
            "vhost": "/",
            "pattern": ".*",
            "apply-to": "queues",
            "name": "max_length_policy",
            "definition": {
                "max-length": 10000
            },
            "priority": 0
        }
    ]
}
